export default function RoiPage() {
  return (
    <main>
      <h1>Hard Return on Investment ($ROI$)</h1>
      <p>Template metric: $ROI$ = (Savings - Cost) / Cost</p>
      <p>TODO: Connect to deterministic backend calculation output.</p>
    </main>
  );
}
