# Backend Template (Thin Agent)

Python architecture is intentionally constrained to deterministic LangGraph execution.

## Included folders
- `agent/`: deterministic node templates
- `telemetry/`: global Paid.ai SDK initialization template
- `settlement/`: mocked Stripe Agentic Commerce Protocol REST endpoints

> Template-only scaffold. Keep logic deterministic and reviewable.
